name: iOS Build

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: macos-15

    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Run the shared iOS build verification script
        run: ./scripts/verify-ios-builds.sh
